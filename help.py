import os
import discord
while True:
   text = input()
   if text == "*help":
      print("*s 'lane' will randomly select a champion from a lane of your choice")
      print("*roll followed by any number of names will generate a random lane for each person. Remaining lanes will be assigned to Random.") 
